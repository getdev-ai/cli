[tool.poetry]
name = "fixture"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.11"
django = "^4.2"

[tool.poetry.group.dev.dependencies]
black = "^24.0"

[tool.poetry.dev-dependencies]
mypy = "^1.8"
