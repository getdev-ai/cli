[project]
name = "fixture"
version = "0.1.0"
dependencies = ["requests>=2.31.0"]

[project.optional-dependencies]
test = ["pytest>=7.0"]
lint = ["ruff>=0.1"]

[dependency-groups]
dev = ["black>=24.0"]
