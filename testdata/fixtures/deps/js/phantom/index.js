const fs = require("node:fs");
const path = require("path");
const _ = require("lodash");
const bogus = require("totally-fake-package-xyz");
const utils = require("./utils");

module.exports = { fs, path, _, bogus, utils };
