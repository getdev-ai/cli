// Second-level barrel — its own `export *` chain is what pushes eq/and/sql
// past the one-level resolution boundary, so the root surface is Dynamic.
export * from './expressions/index.js';
export * from './sql.js';
