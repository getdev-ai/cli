// The `drizzle-orm/pg-core` SUBPATH surface, resolved via
// `exports["./pg-core"].types`. Declared directly (no further star chain) so
// the subpath resolves to a trusted `Resolved` surface and `pgTable` verifies.
export declare function pgTable(name: string, columns: unknown): unknown;
export declare function integer(name: string): unknown;
export declare function text(name: string): unknown;
