// Mirrors drizzle-orm's real root barrel: eq/and/sql live >1 `export *` level
// deep (root -> ./sql -> ./sql/expressions + ./sql/sql). A single-level star
// resolver cannot fully enumerate this, so the surface correctly degrades to
// Dynamic (an info note), never a false High for `{ eq, and, sql }`.
export * from './sql/index.js';
