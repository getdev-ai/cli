// Mirrors winston: the oldest shape — no exports map, no `module`, a hand-
// written bundled `.d.ts` at the package root referenced by the top-level
// `types` field, with plain declaration exports (no `export =`, no star
// chain). Fully resolves to a trusted `Resolved` surface — `format`,
// `createLogger`, `transports` verify.
export declare const format: unknown;
export declare function createLogger(options?: unknown): unknown;
export declare const transports: unknown;
export declare const config: unknown;
export declare const addColors: unknown;
