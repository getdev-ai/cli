// Mirrors @aws-sdk/client-s3: a SCOPED package typed via the classic top-level
// `types` field, whose entry is a one-level RELATIVE barrel. The re-export
// targets declare their symbols directly (no further chain), so the surface
// fully resolves to a trusted `Resolved` — `S3Client`/`GetObjectCommand` verify.
export * from './S3Client';
export * from './commands/index';
