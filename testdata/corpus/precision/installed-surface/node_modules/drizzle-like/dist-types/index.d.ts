// The REAL type surface, declared only via `exports["."].types` in
// package.json (there is no top-level `types`/`typings` field). A pre-13-03
// getdev never read the exports map and instead guessed the alphabetical-first
// top-level `misc.d.ts` (the decoy sibling), enumerating a wrong/partial
// surface as a confident `Resolved` one — the Seava nonexistent-api FP class.
// Post-fix, `locate_types_entry` resolves THIS file (PREC-03/D-06) as a trusted
// `Resolved` surface, so the real named imports below verify against it.
export function eq(left: unknown, right: unknown): boolean;
export function and(...conditions: unknown[]): boolean;
export const sql: unknown;
