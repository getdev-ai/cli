// Minimal harness-only surface stub (FP-sentinel scaffolding, docs/TESTING.md
// "the corpus") — declares only the members this trimmed snapshot actually
// calls, so getdev real's apisurface check resolves them as present instead
// of flagging every framework member as a nonexistent-api false positive
// (these members do genuinely exist on the real express package).
export function json(): unknown;
export function urlencoded(options?: unknown): unknown;
export function Router(): unknown;
