// Minimal harness-only surface stub (FP-sentinel scaffolding) — see
// testdata/corpus/README.md. Declares only the members this trimmed
// snapshot actually references (these genuinely exist on the real
// `next` package).
export const NextResponse: unknown;
