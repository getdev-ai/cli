// Minimal harness-only surface stub (FP-sentinel scaffolding) — see
// testdata/corpus/README.md and the sibling `server.d.ts`. The real `next`
// package's root entry point does not itself export `NextResponse` (it
// lives under the `next/server` subpath) — kept empty here to accurately
// mirror that shape (audit A13).
