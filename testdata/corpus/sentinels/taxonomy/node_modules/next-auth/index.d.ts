// Minimal harness-only surface stub — see node_modules/next/server.d.ts.
// The real `next-auth` package's root entry point does not itself export
// `getToken`/`withAuth` (those live under the `jwt`/`middleware` subpaths,
// see the sibling `.d.ts` files) — kept empty here to accurately mirror
// that shape (audit A13).
