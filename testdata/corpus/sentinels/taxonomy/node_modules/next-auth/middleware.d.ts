// Minimal harness-only surface stub — see node_modules/next/server.d.ts.
// Mirrors the real `next-auth/middleware` subpath entry point (audit A13: a
// subpath import is judged against its own `.d.ts`, not the package root).
export function withAuth(...args: unknown[]): unknown;
