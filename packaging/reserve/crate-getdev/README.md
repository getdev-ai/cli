# getdev

> Verify, secure, and ship AI-generated code. One binary, runs locally, nothing leaves your machine.

**Placeholder release reserving the crate name** while the CLI is in development.
From v0.1, `cargo install getdev` (or `cargo binstall getdev` for prebuilt binaries)
installs the real tool.

- Site: https://getdev.ai
- Source (Apache-2.0): https://github.com/pzelenin/getdev-cli
