---
name: False positive
about: getdev flagged something it shouldn't have
labels: false-positive
---

<!-- False positives are existential for getdev — thank you for reporting instead of just uninstalling. -->

**Rule ID** (e.g. `audit/cors-wildcard`):

**The flagged code** (trimmed to the relevant part; redact anything sensitive):
```
```

**Why it's a false positive**

**Finding output** (the JSON object from `getdev <cmd> --json`, or the terminal block):
```text
```

**Environment**
- getdev version:
- Language/framework:

<!-- Workaround while this is open: suppress with a reason —
     inline: // getdev-ignore <rule-id> -- <reason>
     or a [[suppress]] block in .getdev.toml -->
